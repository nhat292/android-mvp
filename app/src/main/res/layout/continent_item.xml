<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/llItem"
    android:layout_width="match_parent"
    android:layout_height="@dimen/continent_item_height"
    android:clickable="true"
    android:focusable="true"
    android:foreground="?attr/selectableItemBackground"
    android:orientation="vertical">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:gravity="center_vertical"
        android:orientation="horizontal">

        <ImageView
            android:id="@+id/imgContinent"
            android:layout_width="@dimen/global_item_size"
            android:layout_height="@dimen/global_item_size"
            android:layout_marginStart="@dimen/margin_padding_small"
            android:src="@drawable/ic_global"
            android:tint="@color/colorPrimary" />

        <TextView
            android:id="@+id/txtContinent"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginEnd="@dimen/margin_padding_small"
            android:layout_marginStart="@dimen/margin_padding_small"
            android:maxLines="1"
            android:singleLine="true"
            android:text="ASIA"
            android:textColor="@color/accent_black" />
    </LinearLayout>

    <View
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:layout_marginStart="@dimen/margin_padding_small"
        android:background="@color/accent_black" />

</LinearLayout>